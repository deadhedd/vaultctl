# Review, main, 2026-09-21

**Reviewed by**: gpt-5 (author model unavailable)
**Scope**: 5 files, uncommitted
**Verdict**: Approve

## Summary

This change upgrades external documentation state to a complete version 2 ownership inventory and validates the committed projection and worktree before source fetch and again before projection writes. Replacement, staging, commit confirmation, and recoverable rollback now use distinct exact file sets, preserving paths outside the prior and next inventories. The implementation matches the governing safety requirements, and the disposable repository tests cover the meaningful failure and ownership boundaries.

## Strengths

- The prefetch filesystem and committed tree checks reject incomplete or unexpected ownership evidence before source mutation or vault writes.
- `externalRefreshFilePaths` limits staging, commit confirmation, index restoration, and worktree restoration to exact files, while directories remain filesystem only as required.
- The tests cover baseline time mutation, unowned descendants after the final scan, stale path rollback, and unowned content injected during rollback.

## Test coverage

The configured Go suite provides meaningful coverage for the new version 2 parser, inventory validation, stale cleanup, type changes, prewrite baseline validation, exact staging, and recovery behavior. Independent review verification passed `go test ./internal/vaultctl -run 'TestRefreshExternalDocs|TestValidateScannedDestination|TestParseExternalState' -count=1`, `go test ./...`, `go vet ./...`, and `git diff --check`.
